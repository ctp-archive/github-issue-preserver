---
layout: layouts/default.njk
homeLink: ''
---
{% include 'header.njk' %}
<main>
  <p>This is an archive of the issues, pull requests, comments, and file attachments of the included COVID Tracking Project GitHub repository. You can also <a href="download.html">download this data as JSON files</a>.</p>
  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th>Type</th>
        <th>Title</th>
        <th>Comments</th>
        <th>Author</th>
        <th>Labels</th>
      </tr>
    </thead>
    <tbody>
      {% for issue in issues %}
        <tr>
          <td>{{issue.created_at | ghdate}}</td>
          <td>
            {% if issue.pull_request %}
              Pull Request
            {% else %}
              Issue
            {% endif %}
          <td>
            <strong>#{{issue.number}}</strong>
            <a href="issue/{{issue.number}}.html">{{issue.title}}</a>
          </td>
          <td>{{issue.comments}}</td>
          <td>
            <strong>{{issue.user.login}}</strong>
          </td>
          <td>
            {% for label in issue.labels %}
              <span class="label">
                <a href="label/{{label.id}}.html">{{label.name}}</a>
              </span>
            {% endfor %}
          </td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
</main>
